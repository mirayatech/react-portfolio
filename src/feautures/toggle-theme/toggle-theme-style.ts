import styled from 'styled-components'
import { flexCenter } from '../../library'

export const Button = styled.button`
  ${flexCenter}
  padding: 0.3rem;
  font-size: 1.25rem;
  border-radius: 0.3rem;
`
